[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "paged-inference-engine"
version = "1.0.0"
dependencies = ["torch==2.11.0", "triton>=3.5.0", "numpy>=2.5.0"]

[tool.setuptools]
packages = ["core", "kernels", "transformer"]